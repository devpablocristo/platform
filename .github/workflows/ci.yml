name: CI

# Placeholder. La matriz real (build/test por lenguaje, validate-boundaries,
# validate-module-versions) se rellena en Fase A6/A9 cuando el workspace
# tooling y los scripts estén portados desde core/scripts y modules/scripts.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Bootstrap placeholder
        run: |
          echo "CI matrix pending — see docs/migration/MIGRATION_FROM_CORE_MODULES.md"
          echo "Phase A6 will populate go.work / pnpm-workspace.yaml / Cargo.toml"
          echo "Phase A9 will port publish-ts-package.yml and publish-python-package.yml"

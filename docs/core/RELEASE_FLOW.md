# Release flow legacy `core`

Este documento quedo reemplazado por el flujo vigente de `platform`:

[`../platform/RELEASE_FLOW.md`](../platform/RELEASE_FLOW.md)

El repo `core` fue fusionado en `platform`; no publicar nuevos paquetes desde
paths o nombres `core-*` salvo un hotfix legacy aprobado explicitamente.

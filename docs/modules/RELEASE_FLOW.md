# Release flow legacy `modules`

Este documento quedo reemplazado por el flujo vigente de `platform`:

[`../platform/RELEASE_FLOW.md`](../platform/RELEASE_FLOW.md)

El repo `modules` fue fusionado en `platform`; no publicar nuevos paquetes desde
paths o nombres `modules-*` salvo un hotfix legacy aprobado explicitamente.

# Versionado legacy `modules`

Este documento quedo reemplazado por el versionado vigente de `platform`:

[`../platform/VERSIONING.md`](../platform/VERSIONING.md)

Los tags nuevos deben usar paths `platform`, por ejemplo:

```text
features/scheduling/go/v0.2.0
ui/modal/ts/v0.1.1
```
